#include <stdio.h>
#include <string.h>

int main() {
    char cadena[] = "Hola Ruben. Felices fiestas.";
    int longitud = strlen(cadena);
    printf("La longitud de la cadena es: %d\n", longitud);
    return 0;
}